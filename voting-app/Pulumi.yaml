name: voting-app
runtime:
  name: python
  options:
    virtualenv: venv
description: Voting app that uses containers
config:
  pulumi:tags:
    value:
      pulumi:template: https://github.com/pulumi/examples/blob/master/aws-py-voting-app/README.md
